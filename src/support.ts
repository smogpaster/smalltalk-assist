/**
 * Tip link shown in the app ("Support the app"). Empty = the section is
 * hidden, so a placeholder never ships. Set this to your Ko-fi page.
 */
export const TIP_URL: string = 'https://ko-fi.com/smogpaster'
